apiVersion: v1
kind: Pod
metadata:
  name: pod-reader-test
  namespace: default
spec:
  serviceAccountName: gihyo-pod-reader
  containers:
  - name: kubectl
    image: bitnami/kubectl:latest
    command:
    - sleep
    - "3600"
  restartPolicy: Never
